# API Base URL for Production
NEXT_PUBLIC_API_BASE_URL=https://api.yourproductiondomain.com/v1

# Mock Data Endpoints (often not used in production, but included for example)
# In a real app, these would point to actual endpoints or not exist.
# We'll set them to a non-existent path or a live API if needed.
NEXT_PUBLIC_MOCK_MEDALS_API=/api/live/medals
NEXT_PUBLIC_MOCK_USERS_API=/api/live/users

# Internal API Key (NOT client-side exposed)
SECRET_ANALYTICS_KEY=prod_analytics_ABCD